version: '3.3'
services:
  app:
    build:
      context: ./
    ports:
      - "8083:80"
    volumes:
      - ./dist:/usr/share/nginx/html

networks:
  default:
    external:
      name: external-iot-power-meter
